# meeting-prompter — Active Progress
# Stays LEAN. Only active/in-progress state here.

current_phase: "liquid-rearchitecture (Wave A landed)"
current_focus: "Wave A code refactor complete on branch liquid-rearch (unpushed). Encoder intelligence layer, attribution hierarchy, retrieval swap all landed test-gated. Needs review + a live call; head training (Wave B) blocked on the forge bidirectional-encoder change."

active_work:
  - id: "WS-14"
    name: "Live dual-stream test (mic + per-app capture)"
    status: pending
    notes: "Still needs a live Zoom/Teams call — now also validates F-604 segmentation, F-601/F-606 attribution, F-607 corrections, and promotes F-507 (Extract) + F-508 (persistent ASR)."

blockers:
  - id: "forge-bidirectional-encoder"
    blocks: "F-503/F-504/F-505 (encoder-head training)"
    notes: "forge trainer templates load the base causally; ADR requires bidirectional encoder. Land + verify the forge encoder-classifier change first."

pending_decisions:
  - "Review liquid-rearch branch (12 commits, unpushed) before merge to main."

recent_sessions:
  - id: "session-017"
    date: "2026-07-20"
    focus: "Liquid re-architecture Wave A (code waves only, unattended) — branch liquid-rearch"
    completed:
      - "F-502 retrieval swap → LFM2.5-Embedding-350M (config-driven embedder, stale-dim purge; gate held Hit@1 94.4%/MRR 0.972)"
      - "F-509 hardened chunk-level retrieval eval (confusable corpus, query/passage prompts; MiniLM vs LFM within 1 query — no clear winner)"
      - "F-501 EncoderIntelligenceLayer + typed TurnState + Head interface (heuristics as first heads; zero behavior change)"
      - "F-510 frozen-encoder linear-probe head, wired-but-off (probe 0.886 macro-F1 but regresses question class → heuristic stays default; caught+fixed a data leak)"
      - "F-601 AttributionResolver (L1/L3/L4 + regime + honest degradation); F-606 conference-room 'Others (room)' + low_confidence flag end-to-end"
      - "F-604 diarization: speaker-change segmentation + roster-bounded clustering; F-605 voice enrollment (store+match+persistence, unit-validated)"
      - "F-506 route-first hot/cold execution; F-507 structured notes via Extract path (plumbed, instruct default); F-508 warm-model runtime; F-607 lexical speaker-consistency corrections"
      - "704 Python tests (+119) + 10 slow gates + 16 frontend, tsc clean"
    next_recommended: "Review + merge liquid-rearch; run a live call; land the forge bidirectional-encoder change to unblock Wave B head training"
  - id: "session-016"
    date: "2026-03-09"
    focus: "WS-14 dual-stream pipeline hardening — thread safety, cleanup, robustness"
    completed:
      - "TranscriptBuffer thread safety: threading.Lock on all public methods (add_chunk, on_silence, flush, reset, all_turns, turn_count)"
      - "Session pipeline cleanup: try/finally for mic capture, pipeline finally stops mic, _thread_safe_put logs drops"
      - "SystemAudioCapture: _capture_error init in __init__, temp WAV cleanup in try/finally"
      - "Bounded _trigger_history with deque(maxlen=1000)"
      - "5 thread safety tests (concurrent add_chunk, silence, flush, reads, source attribution)"
      - "CLAUDE.md: added dual-stream architecture, per-app capture paths, updated test count"
    next_recommended: "Live dual-stream test with Zoom/Teams call, then F-111 section-aware chunking"
  - id: "session-015"
    date: "2026-03-07"
    focus: "F-110 RAG quality audit + F-201 rhetorical question detection"
    completed:
      - "F-110: eval harness (21 queries, Hit@1=94.4%, MRR=0.972), fixed FTS5 schema/triggers/queries, confidence calibration, re-ranking wired, threshold tuned"
      - "F-201: 3 suppression layers (tag questions, self-answering, rhetorical forms), 46 tests"
      - "399 Python tests + 5 eval tests passing"
    next_recommended: "Live dual-stream test, then F-111 section-aware chunking or F-112 result dedup"
  - id: "session-014"
    date: "2026-03-07"
    focus: "ColBERT → hybrid FTS5 + vector RAG migration"
    completed:
      - "Replaced ColBERT with hybrid FTS5+vector pipeline (all-MiniLM-L6-v2, 80MB)"
      - "New lib/rag/ library: storage, parser, chunker, index, retrieval, rank, embedder"
      - "348 Python tests passing"
    next_recommended: "F-110 RAG quality audit + tuning"

completed_summary:
  total_items: 56
  total_sessions: 16
