# Real-Time Meeting Intelligence Agent
# Core dependencies

# Audio processing
sounddevice>=0.5.0
soundfile>=0.12.0
scipy>=1.10.0
numpy>=1.24.0

# PDF parsing for RAG
pypdf>=4.0.0

# System monitoring
psutil>=5.9.0

# LLM inference (for answer generation)
llama-cpp-python>=0.3.0

# ColBERT embeddings for semantic RAG
pylate>=1.0.0

# Configuration
pyyaml>=6.0

# Speaker diarization (Tier 2 — optional, falls back to Tier 1 without these)
torch>=2.0
speechbrain>=1.0

# API server (for Tauri app backend)
fastapi>=0.110.0
uvicorn[standard]>=0.27.0
python-multipart>=0.0.6
